# mad-science-scraper
What do you do when you can't do mad science? You scrape it.

# Usage
 - Rename `config.yml.example` to `config.yml`
 - Fill in your values for `config.yml`
 - Run `src/scraper.py`
 - Let the scraping commence